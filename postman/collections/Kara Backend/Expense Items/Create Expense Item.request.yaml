$kind: http-request
url: "{{baseUrl}}/expense_items"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "expenseId": "{{expenseId}}",
      "name": "Item Name"
    }
order: 1000
